[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    smoke: Quick sanity tests (reference SDK imports from all others)
    full: Comprehensive tests (all SDK permutations)
